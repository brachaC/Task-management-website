import { AppOverHilightDirective } from './app-over-hilight.directive';

describe('AppOverHilightDirective', () => {
  it('should create an instance', () => {
    const directive = new AppOverHilightDirective();
    expect(directive).toBeTruthy();
  });
});
